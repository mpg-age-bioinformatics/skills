interface:
  display_name: "Bioinformatics Sandbox"
  short_description: "Set up an isolated bioinformatics Docker Sandbox"
  default_prompt: "Use $bioinformatics-sandbox to initialize this repository for bioinformatics work in a Docker Sandbox, with workload containers run inside the sandbox when needed."
