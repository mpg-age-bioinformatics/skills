# Project instructions

Follow `AGENTS.md` for every code-generation, analysis, testing, container, data, and reproducibility task in this repository. Work in the Docker Sandbox directly for lightweight tasks, and launch pinned workload containers on its private Docker daemon when project dependencies or stronger runtime isolation are needed.
